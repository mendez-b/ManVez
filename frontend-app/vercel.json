{
  "rewrites": [
    { "source": "/api/mangadex/:path*", "destination": "https://api.mangadex.org/:path*" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "\\/(.*)", "dest": "/index.html" }
  ]
}